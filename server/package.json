{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tsx watch src/server.ts",
    "build": "tsup src --format esm",
    "start": "prisma migrate deploy && node dist/server.mjs",
    "wait-api": "for i in {1..30}; do if curl -f $1 > /dev/null 2>&1; then echo 'API is ready'; exit 0; fi; echo 'Waiting for API...'; sleep 5; done; echo 'API failed to start'; exit 1",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "prisma": {
    "seed": "tsx prisma/seed.ts"
  },
  "description": "",
  "devDependencies": {
    "@faker-js/faker": "^9.9.0",
    "@types/node": "^22.18.0",
    "cypress": "^14.5.4",
    "dotenv-cli": "^10.0.0",
    "prisma": "^6.7.0",
    "tsup": "^8.4.0",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  },
  "dependencies": {
    "@fastify/swagger": "^9.5.1",
    "@fastify/swagger-ui": "^5.2.2",
    "@prisma/client": "^6.7.0",
    "dotenv": "^17.2.1",
    "fastify": "^5.3.2",
    "fastify-type-provider-zod": "^4.0.2",
    "zod": "^3.24.4"
  }
}
